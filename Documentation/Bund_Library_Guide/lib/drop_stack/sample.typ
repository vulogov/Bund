#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Drop stack "TheStack"
  //
  :TheStack drop_stack
  // Now stack _TheStack_ doesn't exists
  ```
]
