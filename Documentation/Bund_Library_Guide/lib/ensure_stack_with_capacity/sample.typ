#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Set stack "StackName" current
  // and set stack capacity to not more than
  // 128 values
  //
  128 "StackName" ensure_stack
  ```
]
