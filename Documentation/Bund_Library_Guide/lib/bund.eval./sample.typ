#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Evaluate code snippet from the sting
  // dynamically created and stored in the workbench
  //
  "2 " "2 +" + . bund.eval.
  // Leaves 42 on the stack
  ```
]
