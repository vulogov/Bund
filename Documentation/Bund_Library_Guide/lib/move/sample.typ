#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  //
  //
  @A
  @main
    42 :A move
  @A
    42 == { "Data moving from @main to @A happens"
            println } if
  ```
]
