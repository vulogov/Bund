#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Converting data in stack to string
  //
  :TRUE convert.to_bool
  TRUE == {
    "Conversion is succesful"
    println
  } if
  ```
]
