#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  42 . convert.to_string take
  "42" == {
    "Conversion is succesful"
    println
  } if
  ```
]
