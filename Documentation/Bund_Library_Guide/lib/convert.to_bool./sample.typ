#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  1 . convert.to_bool take
  TRUE == {
    "Conversion is succesful"
    println
  } if
  ```
]
