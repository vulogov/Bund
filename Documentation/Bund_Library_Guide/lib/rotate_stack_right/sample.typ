#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Rotate stack :A to the right and do the math
  //
  @main
  @A
    1 41 3
  @main
  :A rotate_stack_right + println
  // Printing result of + operation = 42
  ```
]
