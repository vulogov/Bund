#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Evaluate code snippet from the sting stored in the stack
  //
  "2 2 +" bund.eval
  // Leaves 42 on the stack
  ```
]
