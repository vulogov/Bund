#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Swapping values
  //
  1 2 swap_one
  // As the result, we will have following state in the stack 2 1
  ```
]
