#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  @main
  @StackName
    1 2 3
  @main
    :StackName 3 dup_many_in
  // Duplicate all three values in
  // stack "StackName"
  ```
]
