#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Converting data in stack to int
  //
  TRUE convert.to_int
  1 == {
    "Conversion is succesful"
    println
  } if
  ```
]
