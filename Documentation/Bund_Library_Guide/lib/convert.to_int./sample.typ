#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  42.0 . convert.to_int. take
  42 == {
    "Conversion is succesful"
    println
  } if
  ```
]
