#import "@preview/codelst:2.0.2": sourcecode


#sourcecode(
  numbers-side: right,
  gutter: 2em,
)[```rust
  //
  // Duplicate data in stack
  //
  42 41 2 dup_many
  // Now we have 42, 42, 41, 41 in stack
  ```
]
