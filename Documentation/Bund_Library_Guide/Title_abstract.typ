#import "@preview/unify:0.6.0": num

#align(right)[
  This book serves as a reference guide for the BUND functions (or "words") defined in standard library.
]
#align(right)[
Referencing #text(blue)[#num(csv("index.csv").len())] functions.
]
